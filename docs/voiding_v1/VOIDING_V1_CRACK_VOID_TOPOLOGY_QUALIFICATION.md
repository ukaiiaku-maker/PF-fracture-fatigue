# Crack–void topology qualification

Gate: **CRACK_VOID_TRANSACTION_QUALIFIED**

`CRACK_TO_VOID_LIGAMENT` starts at the current sharp tip, follows a supplied admissible cleavage direction, and ends at the first circle intersection. A miss creates no candidate. Its only barrier identity is the caller's existing cleavage barrier. Event length is the intact tip-to-near-surface ligament; void space is zero fractured length.

Connection changes cavity status and component ownership atomically. Injected verifier failures reproduce the pre-trial SHA-256 fingerprint exactly. A connected cavity remains dormant until a separate downstream first passage creates a renewed sharp front. Smooth-surface driving is direct local tensor traction; analytical `K/sqrt(r_tip)` amplification is explicitly false.

Fractured ligament, pre-existing free span, active-front movement, their projected measures, and total connected free-surface extent are independent ledger fields.
