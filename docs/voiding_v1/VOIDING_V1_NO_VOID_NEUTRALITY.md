# No-void neutrality

Gate: **NO_VOID_BASELINE_PASS**

`VoidingConfig.enabled` defaults to false. The existing sharp-front entry points are unchanged and do not import or instantiate the V1 registry. Therefore the disabled capability does not create a site, evaluate a hazard, advance an RNG, alter a mesh, or change a crack event. The empty registry has a deterministic serialization fingerprint and exact copy/restart round trip.

Focused tests prove the default and empty restart contract. Existing canonical artifacts and the four-class registry are untouched. The physical package diff caused by V1 is one new isolated module only; baseline trajectory code has no changed line.
