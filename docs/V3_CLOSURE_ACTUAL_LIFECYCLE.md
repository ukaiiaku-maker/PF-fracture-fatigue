# Actual checkpoint-backed lifecycle qualification

The replacement runner does not import the V2 or V3 campaign runners. It calls
the actual site, cavity growth, conservative remesh, ligament and downstream
transaction APIs. Its frozen inventory includes 45 transition/partition
attempts, 11 restart stages, 12 distinct controlled histories, four V12 versus
disabled-V5 comparator paths, 32 natural seeds under five partitions with
midpoint restart, and reachable promotion/ligament rollback injections.

Actual complete checkpoint objects are restored and fingerprinted by the
lifecycle branch of validate_closure_evidence. Endpoint validity, actual
transition occurrence, restart/neutrality comparisons and conservation are
independently recomputed. Evidence validity is not scientific success.

The last positive-rate site interval is localized from the preserved remaining
clock, and its actual duration is recorded. It is not extended with an epsilon,
given a replacement threshold, or completed with a zero rate. Promotion is a
geometry transaction, not an Arrhenius clock. Ligament partitions leave the
last actual crossing to the existing topology transaction, avoiding duplicate
completion of an already pending event.

No unavailable downstream child is fabricated as a restart source. Such a row
retains the actual connected checkpoint and records that the requested stage
is unavailable. Restart reproduction of the attainable blocked endpoint is
separate from the still-required continued-front terminal gate. The original
successful-operation trace requirements are not weakened to accept a failure
as a successful physical transition.

Controlled limiting histories use accepted FEM loads (0.6 and 0.1 micrometer
opening) and eight actual 1e-10 s growth intervals. Limiter identity is checked
from measured rates, not forced by changing a barrier. Healing competes with
stabilization under a prescribed compressive load; stabilization is not
artificially zeroed to obtain healing. Delayed downstream uses accepted
unload/reload states, not an injected tensile tensor. A physically different
outcome remains a failed intended-history gate.

The disabled-V5 entry accepts only void-free Stage-II V12 states and delegates
to that actual driver. V11 is explicitly rejected as a comparator. This is a
mechanical neutrality screen; software-forced Stage-II event diagnostics do
not become evidence of physical cavity cleavage.

Downstream lifecycle rollback remains unavailable while first passage is
unqualified. A valid checkpoint-backed audit is not complete scientific
closure or RC readiness.

## Fine boundary ownership repair

The 512/192 production transfer exposed a second independent defect: the 2%
radial identification band contains first-layer *interior* nodes. Sorting all
such nodes invented a polygon and a non-boundary intersection. Ray intersection
and tensor site selection now require actual one-owner mesh boundary edges.
The radial band identifies the component only. The frozen 2% identifier and
all exact topology tolerances are unchanged; interior nodes are not surface
nodes regardless of radial proximity.
