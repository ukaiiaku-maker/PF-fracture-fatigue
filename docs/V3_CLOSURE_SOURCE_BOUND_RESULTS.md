# Source-bound closure results — scientific decision BLOCKED

This checkpoint does not qualify production downstream cleavage, complete
static crack–void mechanics, full lifecycle closure, or RC readiness. The
preserved V2 and blocked V3 campaigns and the original traction A/B trees are
unchanged. All history is linear. No merge or RC branch was created.

## Evidence provenance

All paths below are under `artifacts/voiding_v5_finalization_v3_closure/`.
Each pair is an exact recursive byte match, not a comparison with relaxed
floating tolerances. Producer SHAs are recorded in the source payloads;
these separate executions must not be called one latest-head CI campaign.

| Evidence | Producer checkpoint | Physical content |
| --- | --- | --- |
| `traction_source_a,b` | `ea034e8` | 11 unique macOS source-captured static solves; 69 predicates |
| `traction_linux_a,b` | `123c018` | Linux clean-worker peers from run 34082731068 |
| `mechanics_a,b` | `c225d1b` | 64 unique mechanics solves; 677 predicates |
| `production_a,b` | `6134aa5` | Four actual production resolutions; three accepted connections and one fine-reference rejection |
| `lifecycle_a,b` | `31b3b18` | Historical checkpoint-backed attempts; offset terminal score superseded below |
| `lifecycle_terminal_a,b` | `3cbb11b` | Replacement actual lifecycle runs with strict owned-exit zero-drive classification |

Every mechanics source binds its exact input configuration, realized mesh,
support, assembled sparse system, solved displacement/stress and raw metrics.
The validator reassembles the actual CST system. Every lifecycle endpoint is
a complete restored checkpoint, not a detached state label. Production
validation re-reads actual candidate tensors/rates and replays the guard; it
also reproduces the fine connection exception from the saved accepted state.
Manifest checks precede checkpoint restoration. Checkpoints contain pickle
payloads: load only trusted generated evidence.

## Static findings

The narrow fine adjacent-CST traction result remains PASS:

- Cavity only: 0.0277255; crack plus cavity: 0.0415391 (frozen limit 0.05).
- Fine mesh minimum quality: approximately 0.2266 (frozen minimum 0.05).
- Coupled refinement is approximately first order. Angular-only refinement
  at 48 radial layers worsens recovery; radial refinement improves it.
- The registered numerical shape/resolution screen separates the crossed
  cases. It is a numerical screen, not authorization for a production event.
- Fixed-arc full tensors and fixed physical crack-tip tensors are measured
  with frozen operators and recorded interpolation stencils. The cracked
  256/96 tip error versus 512/192 is 0.08353, failing the 0.05 limit.
- The source-derived Kirsch maximum-concentration check uses the existing
  reaction/width normalization and infinite-plate maximum of 3. The fine
  error is 0.0143504, passing the frozen 0.03 limit. This derived check is not
  an additional physical base solve or a substitute for the full tensor field.

The 64-case matrix has 571 passing and 106 failing predicates. Failures are
42 traction, 22 mesh-tip-tensor, 21 mesh-cavity-tensor, 17 quality, three
perturbation-convergence and one live-edge-owner predicate. The matched
far-void controls pass. All eight energy/compliance derivative comparisons
pass, but three of four perturbation-size convergence comparisons fail.
In particular, crack perturbations reveal support/mesh sensitivity; the
finest crack small-step derivative is nearly zero while the larger-step
derivative is not. No denominator floor or tolerance was changed to hide it.

The independent no-intact-path predicate also requires nonempty two-sided
seed certification; absence of a path alone is not a vacuous PASS. All 64
recorded seed-deficiency lists are empty, so this hardening changes no
observed result.

## Actual production transfer and policy C

The 32/12, 64/24 and 128/48 production runs complete their real promotion,
resolved growth and ligament connection. They retain the same candidate ID
and threshold (1.6231089048573308), a connected/dormant root, and no active
child. The positive unqualified surface source cannot advance hazard, consume
or redraw its threshold, select a proposal, or create a child. Full-state and
restart checks verify the guard rather than merely inspecting a policy label.

The 512/192 matched production reference rejects with
`v12_support_not_certified: INSUFFICIENT_OPPOSITE_SIDE_SEEDS`. Its detailed
boundary certificate reports a tube intersecting the cavity boundary while
only the external-root context is present. The first support rebuild precedes
the inactive cavity-terminal transition. This is a transaction-ordering lead,
not permission to waive an active-tip check. No boundary or interior cut
predicate was relaxed.

Earlier fine-mesh defects were repaired: growth now rebuilds radial layers
instead of moving the cavity boundary through them, and ray intersections
use actual one-owner boundary edges instead of interior nodes falling within
the cavity-identification band. Earlier diagnostic attempts remain in the
temporary campaign directory and are not exact-head attestations.

The old fine static solve is not a matched production reference: material,
growth and plastic history differ. Moreover, the frozen registry supplies
no mesh-transfer barrier/rate acceptance limits. Consequently
`PRODUCTION_RESOLUTION_CAVITY_TENSOR_QUALIFIED = FAIL_CLOSED`. No new limit,
material calibration or injected tensile tensor is used.

## Lifecycle findings and remaining gaps

Each actual run contains 45 transition/partition attempts, 11 restart stages,
12 controlled histories, four V12/disabled-V5 neutrality paths, 160 natural
seed/partition rows and 11 reachable rollback injections: 243 source-bound
rows, not 243 successful histories.

- Thirty transition attempts observe the requested event. Only 20/45 pass
  the combined exact terminal-state and frozen event-time partition gate.
- All eleven direct/restart paths reproduce their attainable terminal.
  They do not reach one common continued-front terminal.
- Six of twelve strict controlled-history outcomes pass. Positive offset is
  not one of them; the actual owned-exit zero-drive control is.
- V12 versus disabled V5 passes all four comparisons. V11 is not the baseline.
- Promotion and ligament rollback pass all eleven exercised injections.
  Prevoid internal fault hooks and downstream rollback are not completed;
  this is not lifecycle-wide rollback qualification.
- All 160 natural midpoint restarts reproduce exactly, but only 32/160
  combined partition/restart gates pass. Hazard roundoff is retained, not
  normalized away.
- Basic inventory, cavity-area, graph/support-tip and fractured-length
  conservation passes all 243 rows. This does not certify every generalized
  length ledger or every intermediate stage's independent topology.

The historical lifecycle decision counted both offset connections as passes
without requiring the frozen terminal classification. That partial score is
superseded by the strict exit-probe predicate and replacement A/B execution.
The positive offset has a real rate around 2.41e-13/s and is classified
`CONNECTED_VOID_UNQUALIFIED_POSITIVE_SOURCE`; it is a valid connection but
not the frozen zero-drive terminal. The negative offset has zero exit drive
and is a valid `CONNECTED_VOID_ZERO_DOWNSTREAM_DRIVE` result. Neither result
qualifies downstream continuation.

Healing, intended diffusion limitation, oblique preparation and continued
fronts retain actual failures/outcomes. Missing predecessor stages are
explicitly unavailable, never fabricated as successful child checkpoints.

## CI and publication

At the published head `123c018993e3fb23134b55d26f244e7706daf95a`:

- Dedicated closure run **34082731068: failure**. Forty-two focused tests,
  Linux A/B generation, exact recursive comparison and source ontology
  passed. Comparing the macOS canonical on Linux failed at exact `K_data`
  reassembly. Artifact upload completed.
- The local workflow repair uses the actual retained Linux A/B canonical;
  it does not relax numerical comparisons. Mechanics, production and
  lifecycle A/B jobs have also been added locally. Their latest-head remote
  execution is **NOT_RUN**, not a green attestation.
- Historical V2 run **34082731021: success**, explicitly pinned to preserved
  V2 source `f20d98f`; it is not current closure certification.
- Repository-wide run **34082731022: failure**, 911 passed, one skipped,
  18 failed. Seven identities match the frozen inherited baseline; eleven
  additional V5 tests expect downstream events now prohibited by policy C.
  Those tests are retained unchanged. CI is **not inherited-only** or green.
- The 6ba4094 local full run had 925 passed, one skipped and 19 failed. The
  additional status-script failure was sandbox-denied `ps`; its unsandboxed
  targeted replay passed. At immutable implementation `3cbb11b`, the focused
  suite passes **75 tests** and the complete unsandboxed local suite has
  **928 passed, one skipped and 18 failed**, matching the seven inherited
  identities plus the eleven policy-C downstream-event expectations. The
  exact logs and JUnit files are retained in `regressions/`.

Further normal publication was rejected by the approval reviewer, including
after read-only verification of PR #63 and the attachment authorization. The
reviewer explicitly required direct chat authorization for PR #63's branch.
No workaround push or PR edit was attempted after that refusal. Remote PR
metadata may therefore lag this local evidence checkpoint.

The next publication requires direct confirmation for a normal, non-force
push to `origin/codex/sharp-front-stateful-voiding-v5-finalization-v2` (draft
PR #63), followed by the dedicated exact-head workflow and a terminal-result
PR update. Publication alone will not remove the scientific BLOCKED result.
