# Source-resolution development findings (nonterminal)

The preserved c4fbd2f V3 result remains COMPLETE_BUT_BLOCKED. Development has
located all 19 fine-source global-quality failures in unchanged triangles near
the old crack tip, roughly 182–183 micrometres from the cavity source. The
current refinement did not create these elements. Missing earlier lineage is
reported as unknown, not reconstructed history.

Interface/geometry-constrained flips increase minimum quality from .03199422 to
.05370467. Actual field transfer, support reconstruction, equilibrium, topology,
inventory, reaction/compliance/energy and fixed-source checks pass in the fine
connected sentinel. One subsequent complete-ring refinement passes the frozen
source screens and prospective same-owned-threshold time budget (about 1.10%
waiting-time transfer error). Coarse sources remain unavailable.

Two implementation defects blocked the first actual child trial: stale cavity
boundary edge IDs after refinement, and a segment-intersection predicate that
reported contact between disjoint coordinate bounding boxes for short edges.
Fresh endpoint contexts and an unchanged-tolerance necessary bounding-box test
allow actual first passage, a sole active child, and one ordinary child-tip
continuation to commit. All rejected development trials remain in temporary
evidence and will be retained in the final publication package. These are not
clean final A/B attestations.

Important limits remain:

- At the retained fixed load the actual nucleation waiting time is approximately
  4.59e12 seconds and the first continuation wait is 7.45e16 seconds. These are
  real integrated-clock events, NOT events within the 16-microsecond natural
  ensemble. No event-time cap, threshold, cleavage law or temperature was changed.
- The legacy incident-CST child probe and cleavage API carry `r_tip` ownership,
  but the probe does not use it as an analytical `K/sqrt(2*pi*r_tip)` stress scale.
  This cannot be claimed as complete analytical moving-tip process-state
  coupling. Keep a separate failed causal-use gate; do not invent a radius
  conversion or replace the source law to make this green. `r_tip != R_void`.
- The recovered practical 32/64/128 tensors fail prospective timing transfer.
  Their native exit coordinates also differ from the 512 reference. No projected
  point is substituted to claim exact physical-source identity.
- The centered new static family has quality-valid 256/512 peers, convergent
  reaction/compliance/energy, raw boundary traction and recovered cavity tensors,
  but fixed-tip tensor error .08353 still fails the frozen .05 limit.
- Historical raw disabled identity remains FAIL. Causal equality is a separate
  audited extension; failed future operations must not be counted as completed
  neutral histories.

## Lifecycle qualification V5 prospective execution contract

Schema `v12.voiding-v5-closure-actual-lifecycle/5` retains all old artifacts and
adds exact accepted-time ticks/subticks, constant-source anchored directional
hazards, exact site integrals and cumulative positive subgrid growth. The natural
32-seed window remains 16 microseconds with unchanged partitions and criteria.
Variable FEM-dependent source changes and resolved-remesh subdivision remain
subject to the full fingerprints, not erased fields.

Transition and common-terminal restart tests localize the actual next event at
fixed load, recording its possibly very long waiting time. They are no longer
conflated with the natural finite-window experiment. The optional fine-history
runner uses an explicitly fixed crack with 512/192 initial resolution for the
positive transition/restart peers and controlled histories. Natural seeds,
healing control, and the genuinely 32-to-64 local-remesh control retain their
separate declared inputs. Fine restart geometry must preserve its configured
resolution through promotion and resolved growth. No earlier 16-us failed row
is rewritten.

Rollback now includes five quality-repair stages and source qualification. The
child-support injection is after the actual child support rebuild, not an earlier
endpoint-conformity rebuild. A stage that is never reached remains a failure.
