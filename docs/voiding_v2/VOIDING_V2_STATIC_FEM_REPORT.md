# Voiding V2 static FEM report

The authoritative split-gate decision is generated by `scripts/qualify_voiding_v2_causal_static.py`.

- `EXPLICIT_CAVITY_ONLY_STATIC_FEM_QUALIFIED = PASS`
- `V11_CAUSAL_CRACK_ONLY_STATIC_FEM_QUALIFIED = OPEN`
- `PRESCRIBED_CAUSAL_CRACK_VOID_INTERACTION_QUALIFIED = NOT_RUN_REQUIRES_CAUSAL_CRACK_ONLY_PASS`
- `EXPLICIT_VOID_MECHANICS_QUALIFIED = OPEN`

## Cavity-only subqualification

The no-crack benchmark uses the production CST assembly and constitutive law with a benchmark-specific symmetric rigid constraint. A pin-location invariance test verifies that the horizontal constraint selects only rigid translation. The actual cavity boundary is connectivity derived and checked by exact node-set equality and bidirectional Hausdorff distance.

Four refinements retain the unchanged 10% recovered-traction tolerance. The two finest strong-form values pass, while the separate weak free-DOF residual is near machine precision. Top/bottom reaction balance and local mirrored `sigma_xx`, `sigma_yy`, and antisymmetric `sigma_xy` checks are reported separately. The plate-size matrix identifies the residual Kirsch difference as a finite-width effect. This evidence is sufficient only for the cavity-only PASS subgate; the mesh is not a qualified production local-remesh implementation.

## Superseded centroid-band diagnostics

Evidence through commit `f0e2959ecf1187685e9d7723b2b4314791f9c353` used a fixed-width centroid-selected P0 strip for crack-containing cases. Those files remain preserved in Git history and are classified only as:

`CENTROID_BAND_EXTENSION_DERIVATIVE_NUMERICALLY_STABLE = DIAGNOSTIC`

They do not qualify V11 crack mechanics, interaction-integral magnitudes, crack virtual derivatives, void derivatives at fixed physical crack geometry, or far crack-void interaction. No tolerance was relaxed and the former 81.7% mismatched-mesh result remains preserved.

## Authoritative V11 causal crack-only benchmark

The replacement benchmark represents the physical crack as the exact graph segment `[p0,p1)` and obtains its P0 stiffness mask exclusively from `causal_segment_support`. It contains no centroid band, radius halo, fixed wake width, inferred centroid tip, or synthetic nodal damage strip. The physical graph endpoint is the interaction-integral tip and the prescribed physical `delta_a` normalizes centered energy/compliance derivatives.

For every mesh the evidence records segment endpoints and length, exact selected element IDs, selected count and area, represented intersection lengths, maximum normal support distance, endpoint support error, and a deterministic support fingerprint. A test proves that the mask used by the V2 adapter exactly equals the V11 selector.

## Interaction contours

The production intrinsic-isotropy interaction integral now accepts an optional exact element-exclusion mask. The option defaults to existing behavior and all existing Williams-field tests continue to pass. The causal adapter uses physical contour radii held constant across refinement.

For each contour it constructs the actual nodal Hermite `q`, identifies every element with nonzero `q`-gradient, reports raw overlap with causal crack elements, excludes those exact elements, and then checks external-boundary clearance, angular coverage, and a minimum active-element count. An inadmissible contour emits `NOT_ADMISSIBLE_AT_CURRENT_RESOLUTION` and no K value.

The current causal crack-only result is decisive but remains OPEN: all proposed contours are admissible, yet the KI plateau, perturbation convergence, and energy-versus-interaction-G agreement do not pass. Consequently no new causal crack-void matrix or localized void-radius derivative is run.

## Scope and reproducibility

The Python 3.12 focused workflow runs the V11 causal-wake and interaction-integral tests, regenerates the authoritative evidence twice, and compares bytes. The exact V11 base reproduces the inherited v10 CI failures; no V2 path is implicated.

No crack-void topology, ligament rupture, nucleation coupling, promotion, resolved growth, fatigue, or material calibration is included. Static work must stop at OPEN until the causal crack-only subgate passes.
