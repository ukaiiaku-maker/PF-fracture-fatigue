# Matched return-load common restart protocol V2

Freeze before generating this additional development result: start from the
existing +4e-7 m production prefix, connect the cavity, accept -4e-7 m opening,
verify every candidate has zero drive, retain the 16-microsecond dormant
interval, and return to the original +4e-7 m opening. Permit the same one-ring
source refinement as the normal qualified positive source. Then localize the
actual downstream first passage and continue the sole child once. All eleven
restart positions must traverse this identical load history to one complete
terminal fingerprint, including the owned V2 protocol cursor.

This isolates interruption/restart at the original tensile load. It does not
qualify the stronger +8e-7 m load used by V1. Preserve V1 and its failed one-
and two-ring source attempts: at `c284427`, waiting-time transfer errors were
0.0259380194 and 0.1037261217, against the unchanged 0.0246950766 limit. No
stronger-load failure is relabeled, and V1 remains independently selectable.
Changing accepted V1 checkpoints into V2 cursors is prohibited.

The return load is the existing source-qualified production load, not a newly
tuned intermediate value. Whether its post-compression material history also
qualifies remains a new measured question. If it fails, preserve that failure.
No source representation, threshold, RNG, rate law, energy/topology predicate,
numerical tolerance, natural-seed window, or static registry changes here.
Lifecycle schema /7 records the additional protocol identity explicitly.
