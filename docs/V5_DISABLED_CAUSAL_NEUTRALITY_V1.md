# Disabled-only future-evolution identity V1

Historical raw full-state identity remains FAIL. This is a new, scoped causal
comparison, not a rewrite of the Stage-II/current byte comparison. Model identity,
all mechanics arrays, graph, support ownership, hazards, thresholds, RNG, event
counters and physical energy ledgers remain in the causal fingerprint.

Consumer audit (including whole-object consumers):

| Differing field | Writers/readers | Disabled-future treatment |
| --- | --- | --- |
| Four `latest_` free-residual, constrained-reaction, top/bottom-balance and energy-identity diagnostics | `equilibrate_fixed_load_with_production_fem` computes them from arrays; `voiding_production_v5.observables` reports them; the new **enabled-only** source-quality transaction tests them | Excluded only in this disabled-only domain; independently recompute mechanics through the actual solver. Enabling voiding is outside this equivalence claim. |
| `junction_process_state/v12_boundary_terminal_certificates` | V12 support installation writes a duplicate certificate report; complete fingerprints/checkpoints retain it | Only empty certificates are canonicalized. Nonempty records remain causal for this conservative comparison. |
| `junction_process_state/v12_graph_support_audit/boundary_terminal_certificates` | Independent verifier builds it; support construction consumes the fresh audit to accept/reject, installation stores it; remesh discards and rebuilds the graph-support audit | Only the empty stored-report extension is canonicalized. No predicate or fresh certificate is bypassed. |
| `v12_support_state/source_commit` | Support constructor stores it; support `provenance_for_remesh` copies it; fingerprints/checkpoints serialize it. Mesh/graph/support validation uses the geometry/material/configuration fields, not this commit label | Retain separately as provenance; exclude this single path, not other source/configuration hashes. |
| Absent `void_state` versus `None` | State isolation, checkpoints and enabled/disabled dispatch inspect it; disabled V5 dispatch delegates to the Stage-II V12 event API only when it is inactive | Normalize inactive absence to null; reject any non-null void state. |

Whole-state consumers are isolation/copy, checkpoint pickle and JSON manifests,
complete-state hashing and evidence reporting. None is removed or changed. The
new harness retains raw normalized states and differences beside causal states.
It records source-code literal-key occurrences to bind this audit to the actual
implementation. A new unreviewed difference remains a failure.

Execute the same four historical geometry/load inputs in separate clean base and
current workers. From each endpoint, take two additional source-measured hazard
passages and actual topology/remesh/equilibrium transactions, then compare a
checkpoint/restart continuation. These are disabled mechanics/kinetics software
neutrality histories using the existing Stage-II event API; its zero-resistance
geometry transaction is NOT reclassified as a newly qualified physical-cleavage
history. Require nonempty identical event histories, identical complete causal
states at every future stage, exact mechanics arrays, thresholds and RNG, and
exact within-worker checkpoint replay. A blocked operation is retained and fails
the full future-evolution gate, even if the preceding states match.
