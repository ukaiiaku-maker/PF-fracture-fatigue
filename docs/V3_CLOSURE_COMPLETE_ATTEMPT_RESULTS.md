# V3 closure: complete-attempt evidence, scientific BLOCKED

This record distinguishes execution, valid evidence, and scientific
qualification. The publication bundle is
`artifacts/voiding_v5_finalization_v3_closure/complete_attempt_20260907/`.
Its source implementation map is authoritative: packaging an existing raw
bundle is not another physical execution. The exact publication-head CI run
and its terminal result are recorded subsequently in draft PRs #63 and #62;
this evidence commit necessarily precedes that workflow.

## Coverage and interpretation

| Requirement | Measured result | Classification |
| --- | --- | --- |
| Source transfer | Four independent production resolutions; all four connections commit. Bounded refinement still has no qualified positive peer. | BLOCKED |
| Complete static mechanics | 64 unique solves, 791 predicates: 683 pass, 108 fail. | BLOCKED |
| Physical transition partitions | 45 attempts; 35 requested transitions occur; 24 strict complete-state partition gates pass. | BLOCKED |
| Common-terminal restarts | All 11 attainable-terminal replay comparisons are exact; required continued-front common terminal is not reached. | BLOCKED |
| Controlled histories | 12 distinct physical configurations, six passing registered outcomes. | BLOCKED |
| Historical V12-disabled neutrality | Four genuine historical/current V12 paths run; strict full-state equality fails. Same-head disabled dispatch is separately 4/4. | BLOCKED |
| Lifecycle rollback | 18/22 new injection stages pass, plus all 11 retained stages. Four downstream stages remain unexercised. | BLOCKED |
| Natural seeds | 32 seeds over 16 microseconds, five partitions each; 160 exact midpoint restarts, 36/160 strict partition/restart gates pass. | BLOCKED |
| Stagewise accounting/topology | All measured accepted endpoints and recorded internal natural stages pass. Downstream child/continuation accounting is not exercised. | Observed PASS; complete-lifecycle BLOCKED |

The full bundle includes the raw source captures, complete restart payloads,
physical inputs, actual operation traces, failed cases, source-bound derived
predicates, manifests, and exact A/B comparison. No failed row is excluded to
make this table green. Local and CI regression counts and failure identities
are reported by their own exact implementation SHA, not combined into one
invented latest-head result.

## Demonstrated source and static envelope

The fixed laboratory crack and realized cavity polygon remain explicit. The
four production levels are 32/12, 64/24, 128/48, and 512/192 boundary/radial
resolution. Their tensors are measured at the same owned candidate source;
segment count alone does not qualify them. Tensor-only relative errors against
the fine production reference are approximately 0.192654, 0.098704, and
0.044207. Passing the last tensor-only comparison is not passing the complete
source predicate. Rate, barrier, and event-time transfer errors are published
as diagnostics because the accepted registry defines no acceptance limits for
them. The fine static traction reference is not a matched production geometry.

The bounded fine ring refinement plus 23 fixed-node, boundary-preserving
interior edge flips reaches traction error approximately 0.03693918, below
0.05, and adjacent tensor change 0.00770237892934 using the current tensor
norm as denominator. It still fails the
unchanged global quality threshold: minimum quality 0.03199422 versus 0.05.
Consequently it cannot activate a hazard, consume a threshold, or create a
child. Coarse and inadequate-shape peers remain explicit negative results.
The last coarse attempt retains traction 0.31006001, normal-spacing ratio
0.05312760 (limit 0.03), and tensor change 0.06609797 (limit 0.05). Its passing
tangential spacing and mesh quality do not erase those failures.
Policy C is not weakened to manufacture the required refined positive peer.

Static failures are traction (42), global quality (17), unique live-edge
ownership (one), quality-valid derivative peers (two), perturbation convergence
(three), mesh crack-tip tensor convergence (22), and mesh cavity-tensor
convergence (21). Closed cavity cycles and exclusion of solid triangles from
the cavity pass in all 42 cavity solves. Area/perimeter mesh-convergence
predicates pass in all 22 comparisons. Failed derivative peers with minimum
quality approximately 0.04609 and 0.04051 do not count as the two required
quality-valid levels. The fixed-crack mirror-offset predicates pass at both
128/48 and 256/96; these static symmetry results are separate from controlled
kinetic terminal classifications. All 64 static no-intact-cross-crack-path
predicates pass.
An additional derived audit restores all four production pre/connected pairs
and independently recalculates topology and conservation, without new FEM
solves. All four pass closed-cycle/component/cut certification and accounting;
active graph and support tips are empty. Its exact source, state, and certificate
fingerprints are retained as a single derived report, not four new executions.

## Physical lifecycle distinctions

Connected negative-offset and zero-drive states are valid connection outcomes,
not downstream-nucleation or continuation passes. Positive-offset connection
is also accepted, but its positive unqualified candidate drive cannot be
misreported as a physically zero-drive surface. A connected root owns no
active graph/support tip. Downstream single-front activation, distinct-direction
event ties, and child-dependent accounting remain unexercised in this closure
campaign; earlier helper or historical test results do not replace them.

The passing controlled outcomes are negative offset, short ligament, long
ligament, accommodation-limited growth, actual embryo healing, and connected
zero drive. Centered continuation, the positive-offset zero-drive terminal,
diffusion-limited growth, delayed downstream activation, oblique continuation,
and refined downstream activation do not pass their registered outcomes.

The oblique path is genuinely oblique and fixed in the laboratory frame. Its
resolved-growth proposal intersects four loaded bottom-boundary edges, not
the root's free-surface component. The support certificate correctly rejects
that proposal. The final accepted checkpoint is geometric promotion, with a
resolved cavity, not an unrelated centered available-site fallback. The
authorized free-boundary exception was not extended to loaded edges.

The four unreached new rollback injections are downstream threshold completion,
child creation, child support rebuild, and child-tip continuation. Returning
the original state without reaching the named failure is not a rollback PASS.
Checkpoint-write injection protects the previously published complete restart.

Strict partition differences include last-bit clock accumulation, last-bit
event-time localization, and subdivision-dependent cavity geometry-generation
counts and radius/inventory arithmetic. These are not removed from fingerprints
or hidden with a looser comparator. All 160 natural windows are physically
advanced without an interval exception; the summed elapsed time is bit-exact
to the requested window in 96/160 rows, with accumulated floating-point time
differences retained in the others. Thirty seeds end resolved and two end
healed at partition one. The
controlled healing seed 12010 was prospectively frozen after exploratory
inspection, as documented in the execution protocol; it is not an unbiased
rate estimate or material calibration.

## Historical neutrality and CI interpretation

The historical comparator uses clean Stage-II V3 commit
`326e3f5973ef623781ab8568798c495a3f68238c`, not V11. The complete comparison
retains model and process/source identity. Mechanical arrays match in the
recorded four paths, but additional energy/certification fields, source commit,
and absent-versus-null void state make strict complete-state equality fail.
These differences are published rather than stripped to obtain PASS.

Repository-wide regression failures are classified individually against the
recorded baseline. Seven are inherited. Eleven are Policy-C-incompatible
coarse downstream expectations, not inherited failures. They remain failures:
no xfail, deletion, or weakened positive assertion substitutes for the missing
qualified-source positive peer. Repository-wide CI is not green.
A restricted local run additionally failed an unrelated status-script test
because the sandbox denied `ps`; that exact report is retained separately.
The test passes with normal process-list permission. The full normal-permission
rerun and the final repository CI are reported independently of this
environment-only failure; the status script was not changed.

Fresh traction A/B is compared exactly. Both peers receive independent raw
source reassembly and ontology validation before a separate strict comparison
against the retained historical Linux canonical artifact. The historical
comparison has failed on raw observable recomputation. Prior diagnostics found
identical mesh/system bytes, unchanged predicate outcomes, and last-bit
solution/recovery differences despite matching recorded package versions.
Unrecorded CPU/BLAS runtime details prevent attributing the cause more narrowly.
The historical bytes, comparator, and numerical limits remain unchanged.

Superseded diagnostics are retained honestly: the 3cd lifecycle run failed
tuple/list canonicalization; the 034 pair had an invalid failed-oblique fallback;
the 1e run completed its rows but failed final schema dispatch. None is
represented as the final paired lifecycle evidence. Repairs are covered by
regressions and the final clean-worker replay.

## Scope and retained history

No force push, rebase, squash, merge, branch deletion, or RC is part of this
publication. Accepted V2/V3 and earlier checkpoint artifacts remain unchanged
in branch history. The model remains single-void, two-dimensional plane strain,
uncalibrated, and not experimentally validated. No multiple-front, fatigue,
removed variational-model, GTN, or diffuse resolved-void campaign is enabled.
`r_tip != R_void`.

The permitted terminal decision after full final CI and PR-ledger publication
is `V5_V3_CLOSURE_COMPLETE_BUT_BLOCKED`, never a release qualification merely
because execution and evidence validation have completed.
