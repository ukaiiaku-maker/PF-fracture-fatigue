# V5 transaction and restart audit

The production runner injects failures at graph edit, remesh, field projection,
support rebuild, equilibrium, energy gate, process-state update, topology
verification, and late veto. Accepted-state fingerprints remain exact for these
ligament-path screens.

Nine checkpoint rows verify serialization and exact fingerprint restoration of
the nontrivial final state. V12 support, crack graph, cavity/site state, tensor
histories, RNG, counters, energy ledgers, and length ledgers are checkpoint-owned.

The current restart matrix labels nine requested lifecycle locations, but only
the final nontrivial-state round trip is independently materialized for every
row. Stage-specific interrupted-versus-uninterrupted continuation remains an
open software-contract item and prevents release-candidate status.
