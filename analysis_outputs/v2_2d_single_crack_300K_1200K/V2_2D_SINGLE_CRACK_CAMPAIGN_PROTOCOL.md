# V2 named 2-D single-crack campaign protocol

This protocol freezes a 12-case `V2_NAMED_PARAMETERIZATION_SINGLE_CRACK_SPATIAL_TRANSFER_TEST`. It compares the six opt-in V2 aliases at 300 K and 1200 K, crystal theta 0 degrees, canonical 1x opening increments (`dU=2.0e-7 m`, `dt=8.4 s`), one front, and 1000 micrometres projected extension.

The execution stack is the audited v10.2.28 direct prescribed-geometry single-crack stack. The V2 adapter calls `load_for(alias, "PF_sharp_front")`, requires alias/exact-candidate full-row parity, and injects the exact direct opening and emission surfaces plus exact cooperative renewal, transport, persistent backstress, and blunting values. The adapter changes parameter binding. It does not replace FEM mechanics, J evaluation, the hazard clock, event-length proposal, energy gate, sharp-wake transaction, mesh policy, or tolerances. The source comparison from canonical parent `c1b3f08d956242844ee1206bf10576f08f8a2859` through the accepted V2 overlay and this child contains the opt-in registry plus additive adapter, diagnostics, orchestration, report, and tests. The only shared-driver edit retains accepted native fields in existing atomic checkpoints and calls the opt-in sparse exporter after a checkpoint commits; it does not enter the solve or acceptance path.

The seed is 3621, recovered from the accepted canonical launcher policy. Every case uses that same seed for common-random-number comparison. No seed stride is applied.

Before launch the launcher requires a clean execution tree, exact alias/candidate/hash identities, byte-identical alias and exact lookup, no active sharp-front worker, a fresh output root, at least 1.3 GiB free, and a valid Python executable. The measured storage basis is three completed canonical theta-zero 1x 1000-micrometre cases of 75.4, 64.7, and 62.1 MiB. Twelve cases estimate about 809 MiB; the gate includes more than 50 percent reserve.

Each accepted boundary is stored in the atomic native combined checkpoint. Portable packages are limited to initial, last pre-first-event, first post-event, first states reaching 250/500/750/1000 micrometres, and terminal below-target state. Coincident current roles share one package. Portable stress remains cell-centered. Moving process-zone quantities remain profiles where they are not spatial fields.

The full scalar history is `PF_MODEL_NATIVE_KJ_DRIVING_TRAJECTORY`. Pre-event states separated by committed physical event groups are exported as `RELOAD_SEPARATED_EFFECTIVE_RESISTANCE_CANDIDATES`. These are model diagnostics, not a conventional R-curve or ASTM toughness. Applied K, global G, and VCCT remain unavailable unless an already-qualified canonical operator supplies them.

A software/output fault may be fixed narrowly and resumed once from the latest accepted checkpoint. A scientific gate, constitutive censor, invalid accepted state, geometry boundary, or fail-closed numerical rejection is terminal and must not be bypassed. Completed cases are never restarted. At most two sharp-front workers may run system-wide.

Raw durable data reside at `/Volumes/Data/Data/Nanopillar_calculation/PF-fracture-fatigue_V2_named_single_crack_theta0_300K_1200K_1000um`. Compact review products reside in `analysis_outputs/v2_2d_single_crack_300K_1200K`.
