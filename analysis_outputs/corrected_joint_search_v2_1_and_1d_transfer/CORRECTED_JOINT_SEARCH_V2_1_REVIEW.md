# Corrected joint search V2.1 review

The accepted V2 numerical results are unchanged. V2.1 removes the ambiguous F2R promotion field, separates candidate and condition counts, publishes full candidate bindings and complete state tables, and freezes deterministic ranking before production transfer. The immutable V1 package retains `LEGACY_F0_INTRINSIC_OPENING_CONTROL`.
